<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.single.pro.mapper.ProductKindMapper">

	<select id="getList" resultType="ProductKindModel">
		select `id`, `name`, `pic_url` as picUrl from `sp_product_kind` where `status`='Y' order by `create_time` asc
	</select>

</mapper>
